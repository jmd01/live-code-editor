import { Box } from "@mui/material";

const Preview = () => {
  return (
    <Box
      sx={{
        height: "100%",
        width: "50%",
      }}
    >
      <h2>Preview</h2>
    </Box>
  );
};

export default Preview;
